---
title: Button
description: "Displays a button component."
---

---

import { Heart, Loader } from 'lucide-react'
import { button} from "@/potions/button"
import Potion from '@/components/Potion.astro'
import DemoTabs from '@/components/DemoTabs.astro'
import VariantDemo from '@/components/VariantDemo.astro'
import { MdxExample } from '@/components/Examples/MdxExample.tsx'

<MdxExample>
  <button className={button()}>
    Button
  </button>
</MdxExample>

## Installation

Install via [VScode](/docs/getting-started/vsc) or copy and paste the potion below into a new file, preferably in a folder called *potions*.

## Potion 

<Potion>
<Fragment slot="contentSlot">
```tsx
import { tv, type VariantProps } from "tailwind-variants";

export const buttonPotion = tv({
  base: "ring-offset-background appearance-none inline-flex items-center justify-center rounded-md font-medium ring-focus transition-transform  duration-100 focus-visible:outline-none focus-visible:ring-2 active:scale-[.97] data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
  variants: {
    intent: {
      primary: "text-primary-content bg-primary hover:bg-primary-400",
      accent: "text-accent-content bg-accent hover:bg-accent-400",
    },
    state: {
      filled: "",
      ghost: "",
      soft: "",
      outline: "",
    },
    size: {
      sm: "h-8 px-2 text-sm",
      md: "text-md h-10 px-4",
      lg: "h-12 px-6 text-lg",
    },
  },
  compoundVariants: [
    {
      intent: "primary",
      state: "ghost",
      className: "border-2 border-primary bg-primary/30 hover:bg-primary",
    },
    {
      intent: "primary",
      state: "soft",
      className:
        "bg-primary-200 text-primary-600 hover:bg-primary-400 hover:text-black",
    },
    {
      intent: "primary",
      state: "outline",
      className:
        "text-primary-content border-2 border-primary bg-transparent hover:bg-primary-500 hover:text-black",
    },
    {
      intent: "accent",
      state: "ghost",
      className: "border-2 border-primary bg-primary/30 hover:bg-primary",
    },
    {
      intent: "accent",
      state: "soft",
      className:
        "bg-primary-200 text-primary-600 hover:bg-primary-400 hover:text-black",
    },
    {
      intent: "accent",
      state: "outline",
      className:
        "text-primary-content border-2 border-primary bg-transparent hover:bg-primary-500 hover:text-black",
    },
  ],
  defaultVariants: {
    intent: "primary",
    state: "filled",
    size: "md",
  },
});

export type ButtonPotionProps = VariantProps<typeof buttonPotion>;

```
</Fragment>
</Potion>

## Components 

### Default

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div className="flex items-center">
      <button className={button()}>
      Button
      </button>
    </div>
</div>

<br />

<DemoTabs solidDisabled vueDisabled>
<Fragment slot="vsc">
```bash
pc-button
```
</Fragment>

<Fragment slot="react">
```tsx
import { buttonPotion, type ButtonPotionProps } from "@/potions/buttonPotion";
import type { ButtonHTMLAttributes } from "react";

export interface ButtonProps
  extends ButtonHTMLAttributes<HTMLButtonElement>,
    ButtonPotionProps {}

export const Button = ({
  children,
  className,
  intent,
  size,
  state,
  ...props
}: ButtonProps) => {
  return (
    <button
      className={button({ intent, size, state, className })}
      {...props}
    >
      {children}
    </button>
  );
};

```
</Fragment>

<Fragment slot="solid">
```tsx
import { Pressable as Button } from "@ark-ui/solid";
import { buttonPotion } from "@potions/buttonPotion";

<Button class={button()}>Button</Button>;

```
</Fragment>

</DemoTabs>

### Icon Button

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div class="flex gap-2 items-center">
    <button className={button()}>
     <Heart className='mr-2'/>
      Icon Button
      </button>
    </div>
</div>

<br />

<DemoTabs solidDisabled vueDisabled>
  <Fragment slot="react">
```tsx
import { Button } from '@/components/button'
import { Heart } from "lucide-react";

<Button>
  <Heart className="mr-2" />
  Icon Button
</Button>;

```
</Fragment>

<Fragment slot="vsc">
```bash
pc-button-icon
```
</Fragment>
</DemoTabs>

### Loader Button

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div class="flex gap-2 items-center">
      <button className={button()} disabled>
        <Loader className="mr-2 h-4 w-4 animate-spin" />
          Loading...
      </button>
    </div>
</div>

<br />

<DemoTabs solidDisabled vueDisabled>
  <Fragment slot="react">
```tsx
import { Button } from '@/components/button'
import { Loader } from "lucide-react";

<Button disabled>
  <Loader className="mr-2 h-4 w-4 animate-spin" />
  Loading...
</Button>;

```
  </Fragment>

    <Fragment slot="vsc">
```bash
pc-button-loader
```
  </Fragment>
</DemoTabs>

## Variants

### Intent

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div className="flex gap-2 items-center">
      <button className={button({ intent: "primary" })}>
      Primary
      </button>
      <button className={button({ intent: "accent" })}>
        Accent
      </button>
    </div>
</div>

<br />

<div className="max-h-72 w-full overflow-y-auto rounded-xl bg-[#1b1e28]">
```tsx
import { Button } from "@/components/button";

<Button intent="primary">Primary</Button>
<Button intent="accent">Accent</Button>
```
</div>

### State 

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div className="flex gap-2 items-center flex-wrap">
      <button className={button({ state: "filled" })}>
      Filled
      </button>
      <button className={button({ state: "soft" })}>
      Soft
      </button>
      <button className={button({ state: "ghost" })}>
      Ghost
      </button>
      <button className={button({ state: "outline" })}>
      Outline
      </button>
    </div>
</div>

<br />

<div className="max-h-72 w-full overflow-y-auto rounded-xl bg-[#1b1e28]">
```tsx
  import { Button } from "@/components/button";

 <Button state="filled">Filled</Button>
 <Button state="soft">Soft</Button>
 <Button state="ghost">Ghost</Button>
 <Button state="outline">Outline</Button>
```
</div>

### Sizes

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div className="flex gap-2 items-center">
      <button className={button({ size: "sm" })}>
      sm
      </button>
      <button className={button({ size: "md" })}>
      md
      </button>
      <button className={button({ size: "lg" })}>
      lg
      </button>
    </div>
</div>

<br />

<VariantDemo solidDisabled vueDisabled>

<Fragment slot="react">

```tsx
import { Button } from "@/components/button";

<Button size="sm">sm</Button>
<Button size="md">md</Button>
<Button size="lg">lg</Button>
```

</Fragment>

</VariantDemo>
