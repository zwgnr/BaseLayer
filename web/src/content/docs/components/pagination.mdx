---
title: Pagination
description: "The Pagination component allows you to navigate through large sets of data by dividing it into smaller chunks, making it easier for users to browse through your application."
arkDocs: https://ark-ui.com/docs/react/components/pagination
---

---

import DemoTabs from '../../../components/DemoTabs.astro'
import Potion from '../../../components/Potion.astro'
import { Pagination } from '../../../ui/React/Pagination/pagination.tsx'

<div class="p-4 w-full flex justify-center items-center bg-surface-2 rounded-md">
    <div className="flex items-center overflow-x-auto">
      <Pagination client:load/>
    </div>
</div>

## Installation

Install via [VScode](/docs/getting-started/vsc) or copy and paste the potion below into a new file, preferably in a folder called *potions*.

## Potion 

<Potion>
<Fragment slot="contentSlot">
```ts
import { tv } from "tailwind-variants";

export const paginationPotion = tv({
  slots: {
    root: "flex justify-center",
    list: "flex list-none gap-1 p-0",
    pageTrigger: " ",
    ellipsis: "min-w-10 inline-flex h-10 items-center px-4 text-sm",
  },
});

```
</Fragment>
</Potion>

## Components 

### Default

<DemoTabs vueDisabled solidDisabled>
<Fragment slot="vsc">
```tsx
pc-pagination 
```
</Fragment>
<Fragment slot="react">
```tsx
import {
  PaginationEllipsis,
  PaginationList,
  PaginationNextPageTrigger,
  PaginationPageTrigger,
  PaginationPrevPageTrigger,
  Pagination as PaginationRoot,
  type PaginationProps,
} from "@ark-ui/react";
import { button } from "@potions/button";

import { paginationPotion } from "@potions/paginationPotion";

const { root, list, pageTrigger, ellipsis } = paginationPotion();

export const Pagination = (props: Partial<PaginationProps>) => (
  <PaginationRoot
    count={5000}
    pageSize={10}
    siblingCount={2}
    className={root()}
    {...props}
  >
    {({ pages }) => (
      <>
        <PaginationList className={list()}>
          <PaginationPrevPageTrigger asChild>
            <button
              className={button({
                className: "bg-surface-4 hover:bg-accent",
              })}
            >
              Previous
            </button>
          </PaginationPrevPageTrigger>

          {pages.map((page, index) =>
            page.type === "page" ? (
              <PaginationPageTrigger
                className={pageTrigger()}
                key={index}
                {...page}
                asChild
              >
                <button
                  className={button({
                    className: "bg-surface-3 hover:bg-accent",
                  })}
                >
                  {page.value}
                </button>
              </PaginationPageTrigger>
            ) : (
              <PaginationEllipsis
                className={ellipsis()}
                key={index}
                index={index}
              >
                &#8230;
              </PaginationEllipsis>
            )
          )}

          <PaginationNextPageTrigger asChild>
            <button className={button({ intent: "accent" })}>Next</button>
          </PaginationNextPageTrigger>
        </PaginationList>
      </>
    )}
  </PaginationRoot>
);


```
</Fragment>

</DemoTabs>


