---
title: Toggle
description: "A toggle button allows a user to toggle a selection on or off, for example switching between two states or modes."
primative: "ReactAria"
docLink: "https://react-spectrum.adobe.com/react-aria/ToggleButton.html"
---

---

import DemoTabs from '../../../components/DemoTabs.astro'
import BaseLayer from '../../../components/BaseLayer.astro'
import { ToggleExample } from '../../../Examples/Toggle/Toggle.tsx'

import { MdxExample } from '@/components/Demos/MdxExample.tsx'

<MdxExample>
      <ToggleExample client:load/>
</MdxExample>

## Installation

Install via [VScode](/docs/getting-started/vsc) or copy and paste the potion below into a new file, preferably in a folder at _components/base_.

## BaseLayer 

<BaseLayer>
<Fragment slot="contentSlot">
```ts
import { ToggleButton, ToggleButtonProps } from "react-aria-components";

import { tv } from "tailwind-variants";

const toggle = tv({
  base: "ring-offset-background focus-focus inline-flex appearance-none items-center justify-center rounded-md bg-surface-2 p-2 font-medium outline-none transition-transform  duration-100 disabled:pointer-events-none disabled:opacity-50 [&[aria-pressed=true]]:bg-primary",
});

export const Toggle = ({
  className,
  children,
  ...props
}: ToggleButtonProps & { className?: string }) => (
  <ToggleButton className={toggle({ className })} {...props}>
    {children}
  </ToggleButton>
);

Toggle.displayName = "Toggle";

```
</Fragment>
</BaseLayer>

## Examples 

### Default

<DemoTabs>
<Fragment slot="vsc">
```tsx
bl-toggle 
```
</Fragment>
<Fragment slot="react">
```tsx
import { Toggle } from "@/components/base/toggle";

export const ToggleExample = () => <Toggle>Mode</Toggle>;

```
</Fragment>

</DemoTabs>


